{
  "name": "api-instagram",
  "version": "1.0.0",
  "description": "Instagram for crawling on AWS Lambda",
  "scripts": {
    "deploy": "sls deploy -v --stage $STAGE",
    "remove": "sls remove -v --stage $STAGE",
    "start": "sls offline start"
  },
  "author": "Harace, Moon",
  "devDependencies": {
    "serverless-offline": "^6.1.5",
    "serverless-offline-ssm": "^5.0.0",
    "serverless-plugin-split-stacks": "^1.9.3",
    "serverless-plugin-warmup": "^4.9.0"
  }
}
